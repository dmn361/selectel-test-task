# Тестовое задание на стажировку в Selectel

## Задание 1

>*Для файла access.log вывести список с суммарным объемом данных,
который передал сервер для каждого IP-адреса в формате:*

*1338 bytes for 1.2.3.4
100500 bytes for 123.23.1.2*

```bash
awk '{bytes[$1]+=$10} END {for (ip in bytes) printf "%d bytes for %s\n", bytes[ip], ip}' access.log
```

## Задание 2

>*Используя lsof необходимо вывести список всех ESTABLISHED TCP
соединений. В качестве ответа на вопрос напишите bash-однострочник.*

```bash
lsof -i TCP -s TCP:ESTABLISHED
```

## Задание 3

>*По скриншоту мониторинга определите причину недоступности сервиса.
Определите время недоступности.*

По скриншоту видно, что количество соединений достигло предела, из-за этого новые соединения отклонялись, downtime приблизительно с 13:00 до 13:15, т.е. 15 минут и всё ещё продолжается.

## Задание 4

>*Ниже представлен манифест Dockerfile, который содержит несколько
недочетов.
В качестве ответа укажите на текущие проблемы, оставив комментарий
по каждой инструкции, и опишите, как их можно исправить, оптимизировав
при этом использование слоев итогового образа.*

```Dockerfile
FROM ubuntu:latest
# нельзя использовать тег latest из-за возможных конфликтов во время
# обновления образа в регистри, нужно задать конкретную версию.
# для оптимизации можно использовать сразу nginx:alpine,
# тогда образ будет из двух слоёв (FROM и COPY), без установки пакетов
# но если нам принципиально использовать ubuntu, то ниже ошибки по докерфайлу с ubuntu
MAINTAINER MyCompany
# устаревшая инструкция, можно использовать label
COPY . /var/www/html
# можно перенести ниже, добавить пользователя после установки пакетов 
# и использовать с флагом --chown=user:user для безопасности 
# (не запускать контейнер с root-пользователем по умолчанию),
# написать под это дело Dockerignore для того, чтобы не тянуть лишние файлы
RUN apt-get update -y
RUN apt-get install -y nginx
# можно было выполнить одним слоем и добавить очистку кеша

CMD ["nginx", "-g", "daemon off;"]
```

## Задание 5

>*Вы отвечаете за доступность проекта. Внезапно на одном из узлов
перестает отвечать веб-сервер Nginx. Пользователи сообщают о том,
что не могут получить доступ к веб-сайту, обслуживаемому этим
веб-сервером.
В качестве ответа опишите действия, которые вы бы предприняли для
диагностики и решения проблемы, чтобы минимизировать время простоя
сервиса.*

1. Посмотрю статус nginx

  `systemctl status nginx`

2. Выведу последние строки `error.log`

  `tail -n 50 /var/log/nginx/error.log`

3. Посмотрю, занимает ли другой процесс данный порт

  `ss -ntulp | grep 80`
  `ss -ntulp | grep 443`

4. Проверю конфиг nginx

  `nginx -t`

5. Перезапущу nginx, проверю, останется ли проблема, не выдаст ли ошибок при перезапуске

  `systemctl restart nginx`

## Задание 6

>*Есть образ с сервисом, который запускается локально командой:
docker run -d -p 8080:80 --name my-app my-app-image
Нужно развернуть его в Kubernetes, чтобы оно было доступно снаружи
кластера, устойчиво к сбоям (например, перезапускалось при падении) и
могло легко масштабироваться. Опишите шаги и манифесты для
развертывания.*

